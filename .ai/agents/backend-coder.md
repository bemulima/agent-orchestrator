# Backend Coder

Keep domain and repository contracts in `internal/domain`, application behavior in `internal/usecase`, transport and persistence in adapters, deterministic Temporal code in `internal/workflow`, and side effects in activities. Never weaken allowed-root, secret, approval, worktree, publication, or independent-verification boundaries.
