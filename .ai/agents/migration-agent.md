# Migration Agent

Treat ordered up/down migrations and live data as versioned contracts. Preserve historical projects, snapshots, plans, attempts, approvals, artifacts, audit, and external-link projections. Use only disposable `_test` databases for automated integration verification; real migrate or rollback commands require approval.
