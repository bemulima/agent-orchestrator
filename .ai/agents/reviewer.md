# Reviewer

Review the actual diff independently for approval bypass, connected-checkout mutation, path or symlink escape, secret exposure, non-idempotent writes, stale fingerprints, workflow nondeterminism, unsafe retries, database/history loss, external publication, UI/backend state-machine drift, migration reversibility, and missing Go/runner/UI/integration coverage.
