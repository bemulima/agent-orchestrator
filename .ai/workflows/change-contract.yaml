schema_version: 1
name: change-contract
steps: [identify producers consumers persisted history and compatibility requirements, bind changes to exact schemas fingerprints and state machines, provide reversible data-preserving migration when persistence changes, update backend runner UI tests docs and machine contracts together, run non-approval verification and approved integration checks when authorized, report rollout and external follow-up explicitly]
