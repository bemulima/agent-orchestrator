schema_version: 1
name: refactor
steps: [state preserved approval idempotency workflow persistence and external-action behavior, establish a passing baseline, refactor one dependency boundary at a time, keep behavior or schema changes separate unless required, run repository verification, inspect the final diff for boundary and safety drift]
