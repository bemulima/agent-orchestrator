schema_version: 1
name: bugfix
steps: [reproduce against a disposable fixture, identify the violated domain approval persistence or workflow invariant, add a focused regression test, implement the smallest compatible fix, run repository verification, update progress and affected contracts, review the actual diff independently]
