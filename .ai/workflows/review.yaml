schema_version: 1
name: review
steps: [read the issue architecture contracts migrations and actual diff, inspect approval path and secret boundaries, inspect repository path worktree write-scope and Git safety, inspect PostgreSQL and Temporal idempotency recovery and history preservation, inspect runner schema verification and retry behavior, inspect HTTP UI and external publication gates, report findings by severity with exact file and line]
