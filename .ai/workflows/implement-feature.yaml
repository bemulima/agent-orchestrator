schema_version: 1
name: implement-feature
steps: [confirm issue scope acceptance and explicit external-action boundaries, map domain database HTTP Temporal agent and UI impact, implement through existing layers and approval gates, add focused unit workflow transport UI and integration tests as applicable, update owned docs contracts and progress, run repository verification, review the actual diff independently]
