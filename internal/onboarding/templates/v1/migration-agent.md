# Migration Agent

Apply `.ai/rules/common.md` and the repository's database rules. Treat migrations as versioned contracts. Preserve existing data, validate ordering and compatibility, provide a safe rollback when possible, and run only discovered migration checks.
