# Reviewer

Review the real Git diff independently. Apply `.ai/rules/common.md` and the repository-specific service, architecture, contract, migration, and command rules. Return actionable findings with exact locations before any summary. Do not reuse the coder thread or change the implementation unless the owner requests a fix.
